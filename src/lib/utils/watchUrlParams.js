import { _app } from '$lib';
// import { meta } from 'tinro';
// const route = meta();


// Watch URL params for changes
export const watchUrlParams = () => {
	// console.log(route);

	// page.subscribe(({ url }) => {
	// 	if (!url && !url?.searchParams) return;

	// 	const backupId = url.searchParams.get('backupId');
	// 	_app.updateSetting('backupId', backupId);

	// 	const storybuilder = url.searchParams.get('storybuilder');
	// 	_app.updateSetting('storybuilder.show', storybuilder);
	// });
};
